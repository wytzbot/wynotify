import React,{useEffect,useMemo,useState} from "react";
import {createRoot} from "react-dom/client";
import {Bell,Home,Users,BarChart3,Menu,X,ChevronDown,ChevronRight,Send,Plus,Settings,Code2,CreditCard,HelpCircle,ShieldCheck,Smartphone,RefreshCw,Sparkles,Activity,Zap,LogOut,Search,Check,Download,Lock,Globe,Copy,Link,ExternalLink,Target,MessageSquare,PanelLeft,BookOpen,Share2} from "lucide-react";
import "./styles.css";
import {authReady,authHeader,registerPush,listenForMessages,auth} from "./firebase";
import {signOut} from "firebase/auth";

const plans=[{id:"free",name:"Free",price:"₦0",usd:"$0",features:["1,000 subscribers","Browser push notifications","Click-through tracking","7-day history"]},{id:"starter",name:"Starter",price:"₦4,500",usd:"$3",features:["5,000 subscribers","Audience tags","Click-through tracking","30-day history"]},{id:"pro",name:"Pro",price:"₦7,500",usd:"$5",features:["25,000 subscribers","Audience tags","Scheduled sending","90-day history"]},{id:"business",name:"Business",price:"₦15,000",usd:"$10",features:["100,000 subscribers","Audience tags","Scheduled sending","365-day history"]}];
const simpleNav=[["Home",Home,"Dashboard"],["Send notification",Send,"Notifications"],["Subscribers",Users,"Subscribers"],["Connect app",Link,"Applications"],["Templates",MessageSquare,"Templates"],["Billing",CreditCard,"Billing"]];
const advancedNav=[["Segments",Users],["Tags",Target],["Analytics",BarChart3],["Notification Performance",Activity],["API",Code2],["SDK",Code2],["Webhooks",Code2],["Integrations",Code2],["Backup & Restore",Globe],["Export Data",Download],["Settings",Settings],["Help Center",HelpCircle],["Contact Support",HelpCircle]];
function apiFetch(action,options={}){const query=String(action).includes("&")?String(action):encodeURIComponent(action);return authHeader().then(h=>fetch(`/api?action=${query}`,{...options,headers:{"Content-Type":"application/json",...h,...(options.headers||{})}}));}
function App(){
 const parsePage=()=>{const raw=decodeURIComponent(location.hash.slice(1)||"");if(raw)return raw.replaceAll("-"," ");if(new URLSearchParams(location.search).get("reference"))return "Billing";return "Dashboard"};
 const [page,setPage]=useState(parsePage),[drawer,setDrawer]=useState(false),[advanced,setAdvanced]=useState(false),[compose,setCompose]=useState(null),[dark,setDark]=useState(localStorage.theme!=="light"),[toast,setToast]=useState(""),[loading,setLoading]=useState(true),[data,setData]=useState({subscribers:0,notifications:0,recent:[],scheduled:[],subscription:{}}),[authError,setAuthError]=useState(""),[search,setSearch]=useState("");
 const notify=m=>{setToast(m);setTimeout(()=>setToast(""),2600)};
 const refresh=async()=>{try{const r=await apiFetch("dashboard");const d=await r.json();if(!r.ok)throw Error(d.error);setData(d)}catch(e){notify(e.message.replace(/Firebase/gi,"notification service"))}};
 useEffect(()=>{document.documentElement.dataset.theme=dark?"dark":"light";localStorage.theme=dark?"dark":"light"},[dark]);
 useEffect(()=>{(async()=>{try{await authReady;await refresh();setLoading(false)}catch(e){setAuthError(e.message);setLoading(false)}})();navigator.serviceWorker?.register("/push-service-worker.js").catch(()=>{})},[]);
 useEffect(()=>{const h=()=>setPage(parsePage());addEventListener("hashchange",h);return()=>removeEventListener("hashchange",h)},[]);
 useEffect(()=>{let off;listenForMessages?.(p=>notify(`${p?.notification?.title||"Notification"}: ${p?.notification?.body||""}`)).then(x=>off=x).catch(()=>{});return()=>off?.()},[]);
 const nav=p=>{setPage(p);location.hash=encodeURIComponent(p.replaceAll(" ","-"));setDrawer(false);setSearch("");};
 const visibleRecent=useMemo(()=>data.recent.filter(x=>!search||`${x.title} ${x.message||""} ${x.audience||""}`.toLowerCase().includes(search.toLowerCase())),[data.recent,search]);
 if(authError)return <Shell><section className="page panel"><div className="eyebrow"><ShieldCheck size={13}/>Setup required</div><h1>Let's connect your workspace</h1><p>{authError.replace(/Firebase/gi,"notification service")}</p><p>Your workspace needs a secure account session before you can send notifications.</p><button className="primary" onClick={()=>location.reload()}><RefreshCw size={16}/>Try again</button></section></Shell>;
 if(loading)return <Shell><section className="page panel"><div className="eyebrow"><Sparkles size={13}/>WyNotify</div><h1>Getting things ready…</h1><p>Connecting your notification workspace.</p></section></Shell>;
 return <div className="app"><aside className={`sidebar ${drawer?"open":""}`}><div className="brand"><span className="logo"><Bell size={18}/></span><b>WyNotify</b><button className="icon mobile" onClick={()=>setDrawer(false)}><X/></button></div><div className="workspace"><span>W</span><div><b>My business</b><small>{data.subscription?.plan?`${data.subscription.plan} plan`:"Free plan"}</small></div><ChevronDown size={15}/></div><div className="search"><Search size={14}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search messages" onKeyDown={e=>e.key==="Enter"&&nav("Notifications")}/></div><nav><div className="navLabel">GET THINGS DONE</div>{simpleNav.map(([n,I,target])=><button className={`navItem ${page===target?"active":""}`} onClick={()=>nav(target)} key={n}><I size={17}/><span>{n}</span></button>)}<button className="advancedToggle" onClick={()=>setAdvanced(!advanced)}><PanelLeft size={16}/><span>Advanced tools</span><ChevronDown className={advanced?"":"rot"}/></button>{advanced&&<div className="advancedNav">{advancedNav.map(([n,I])=><button className={`navItem ${page===n?"active":""}`} onClick={()=>nav(n)} key={n}><I size={15}/><span>{n}</span></button>)}</div>}</nav><button className="navItem signout" onClick={async()=>{await signOut(auth);location.reload()}}><LogOut size={16}/>Sign out</button></aside>{drawer&&<div className="backdrop" onClick={()=>setDrawer(false)}/>}<main className="main"><header><button className="icon mobile" onClick={()=>setDrawer(true)}><Menu/></button><div className="crumb"><span>WyNotify</span><ChevronRight size={13}/><b>{page==="Dashboard"?"Home":page}</b></div><div className="actions"><button className="icon" onClick={refresh} title="Refresh"><RefreshCw size={17}/></button><button className="icon" onClick={()=>setDark(!dark)} title="Theme">{dark?"☀":"☾"}</button></div></header><div className="content">{page==="Dashboard"?<Dashboard data={{...data,recent:visibleRecent}} send={()=>setCompose({})} nav={nav}/>:page==="Billing"?<Billing notify={notify} data={data} refresh={refresh}/>:page==="Applications"?<Applications notify={notify} data={data}/>:page==="Templates"?<Templates send={(draft)=>setCompose(draft||{})}/>:page==="Notifications"?<Notifications data={{...data,recent:visibleRecent}} send={()=>setCompose({})} refresh={refresh}/>:page==="Subscribers"?<Subscribers data={data} notify={notify}/>:page==="Backup & Restore"?<Backup data={data} notify={notify}/>:page==="Export Data"?<ExportData data={data} notify={notify}/>:page==="Settings"?<SettingsPage dark={dark} setDark={setDark}/>:<InfoPage page={page} nav={nav}/>}</div><button className="fab mobile" onClick={()=>setCompose({})}><Plus/></button><Bottom page={page} nav={nav} menu={()=>setDrawer(true)}/></main>{compose&&<Composer initial={compose} capabilities={data.subscription?.capabilities} close={()=>setCompose(null)} sent={async payload=>{try{const r=await apiFetch("send",{method:"POST",body:JSON.stringify(payload)});const d=await r.json();if(!r.ok)throw Error(d.error);setCompose(null);notify(d.scheduled?`Scheduled for ${new Date(d.sendAt).toLocaleString()}.`:`Sent to ${d.sent} device${d.sent===1?"":"s"}.`);refresh()}catch(e){notify(e.message.replace(/Firebase/gi,"notification service"))}}}/>} {toast&&<div className="toast"><ShieldCheck size={16}/>{toast}</div>}</div>}
function Shell({children}){return <div className="app"><aside className="sidebar"><div className="brand"><span className="logo"><Bell size={18}/></span><b>WyNotify</b></div></aside><main className="main"><div className="content">{children}</div></main></div>}
function Dashboard({data,send,nav}){const share=async()=>{const text="WyNotify helps businesses send simple customer notifications.";try{if(navigator.share)await navigator.share({title:"WyNotify",text,url:location.origin});else{await navigator.clipboard.writeText(location.origin);alert("WyNotify link copied");}}catch{}};const delivered=data.recent.reduce((a,x)=>a+(Number(x.successCount)||0),0),attempted=data.recent.reduce((a,x)=>a+(Number(x.count)||0),0),delivery=attempted?`${Math.round(delivered/attempted*100)}%`:"—",hasSubscriber=data.subscribers>0,hasSent=data.notifications>0;return <><section className="welcome"><div><div className="eyebrow"><Sparkles size={13}/>YOUR NOTIFICATION HUB</div><h1>Welcome to WyNotify 👋</h1><p>Send updates, promotions and important messages to your customers — without the technical headache.</p></div><div className="buttonRow desktop"><button className="secondary" onClick={share}><Share2 size={16}/>Share WyNotify</button><button className="primary" onClick={send}><Send size={16}/>Send notification</button></div></section><section className="starter panel"><div className="starterText"><span className="starterIcon"><Zap size={19}/></span><div><b>Your simple setup</b><p>Connect your site or app, get subscribers, then send your first message. WyNotify handles the notification delivery.</p></div></div><div className="steps"><Step n="1" text="Connect" done={hasSubscriber} onClick={()=>nav("Applications")}/><Step n="2" text="Get subscribers" done={hasSubscriber} onClick={()=>nav("Subscribers")}/><Step n="3" text="Send a message" done={hasSent} onClick={send}/></div></section><div className="stats simpleStats">{[["Subscribers",data.subscribers,Users],["Messages sent",data.notifications,Bell],["Delivery rate",delivery,Zap],["Your plan",data.subscription?.plan||"Free",CreditCard]].map(([n,v,I])=><div className="stat" key={n}><div>{n}<span><I size={15}/></span></div><strong>{v}</strong><small>{n==="Your plan"?"Current plan":"Live data"}</small></div>)}</div><div className="cols"><section className="panel"><Head title="Recent messages" action="See all" onClick={()=>nav("Notifications")}/>{data.recent.length?data.recent.slice(0,5).map(x=><Row key={x.id} t={x.title} a={x.audience} s={x.status} c={`${x.successCount||0}/${x.count||0} · ${x.clicks||0} clicked`}/>):<Empty text="You haven't sent anything yet." action="Send your first notification" onClick={send}/>}</section><section className="panel"><Head title="What can I do?"/><div className="quick simpleQuick"><button onClick={send}><Send/><div><b>Send a notification</b><small>Write and send a message now</small></div><ChevronRight/></button><button onClick={()=>nav("Applications")}><Link/><div><b>Connect my website/app</b><small>Get your integration instructions</small></div><ChevronRight/></button><button onClick={()=>nav("Subscribers")}><Users/><div><b>See my customers</b><small>View your subscriber count</small></div><ChevronRight/></button><button onClick={()=>nav("Billing")}><CreditCard/><div><b>Upgrade my plan</b><small>Need more subscribers?</small></div><ChevronRight/></button></div></section></div><section className="panel trust"><ShieldCheck size={18}/><div><b>Built for business owners</b><p>You do not need technical knowledge to use the main WyNotify features. Developer tools are available under <b>Advanced tools</b> when you or your developer need them.</p><div className="buttonRow"><button className="secondary" onClick={()=>nav("Templates")}><MessageSquare size={15}/>Use a template</button><button className="secondary" onClick={()=>nav("Applications")}><Share2 size={15}/>Grow your audience</button></div></div></section></>}
function Step({n,text,done,onClick}){return <button className={`setupStep ${done?"complete":""}`} onClick={done?undefined:onClick}><span>{done?<Check size={14}/>:n}</span><b>{text}</b><ChevronRight size={15}/></button>}
function Head({title,action,onClick}){return <div className="head"><div><h2>{title}</h2><p>Simple workspace view</p></div>{action&&<button className="link" onClick={onClick}>{action}</button>}</div>}
function Row({t,a,s,c}){return <div className="row"><span className="mini"><Bell size={15}/></span><div><b>{t}</b><small>{a||"All subscribers"}</small></div><aside><label className={String(s||"sent").toLowerCase()}>{s||"sent"}</label><small>{c}</small></aside></div>}
function Empty({text,action,onClick}){return <div className="empty"><MessageSquare size={25}/><p>{text}</p>{action&&<button className="primary" onClick={onClick}><Send size={15}/>{action}</button>}</div>}
function Applications({notify,data}){const [busy,setBusy]=useState(false),[registered,setRegistered]=useState(false);const enable=async()=>{setBusy(true);try{await registerPush();setRegistered(true);notify("This browser is connected and ready for notifications.")}catch(e){notify(e.message.replace(/Firebase/gi,"notification service"))}finally{setBusy(false)}};const key=data.registration?.workspaceKey||"";const share=async()=>{const text=`I use WyNotify to send customer notifications. Developer registration key: ${key}`;try{if(navigator.share)await navigator.share({title:"WyNotify integration",text});else{await navigator.clipboard.writeText(text);notify("Developer handoff copied")}}catch{}};return <section className="page"><div className="eyebrow"><Link size={13}/>CONNECT</div><h1>Connect your website or app</h1><p className="lead">Start with a one-click test, or hand the developer section to whoever manages your website or app.</p><div className="connectCards"><div className="connectCard featuredCard"><span className="bigIcon"><Smartphone/></span><div><h2>Test it on this device</h2><p>Allow notifications and use this browser as your first subscriber. It is the quickest way to confirm everything works.</p><button className="primary" disabled={busy||registered} onClick={enable}>{busy?"Connecting…":registered?"✓ Connected":"Connect this device"}</button></div></div><div className="connectCard"><span className="bigIcon"><Code2/></span><div><h2>Developer integration</h2><p>Your customers do not need a WyNotify account. Your developer gets a public registration key, SDK example and endpoint from Subscribers.</p><div className="buttonRow"><a className="secondary" href="#Subscribers"><Code2 size={15}/>Open SDK</a><button className="secondary" onClick={share}><Share2 size={15}/>Share with developer</button></div></div></div></div><div className="notice"><ShieldCheck size={17}/><div><b>Owner-friendly setup:</b> you manage subscribers and messages here. Your developer only handles the small integration step.</div></div></section>}
function Templates({send}){const items=[['Promotion','Weekend sale 🎉','Give your customers a reason to come back today.'],['Announcement','Important update','Share a short update your customers should know about.'],['Welcome','Welcome! 👋','Thanks for joining us. We are glad to have you here.'],['New product','Something new is here','Tell your audience about your latest product, service or feature.'],['Reminder','Quick reminder','A friendly reminder about something important.']];return <section className="page"><div className="eyebrow"><MessageSquare size={13}/>QUICK START</div><h1>Notification templates</h1><p className="lead">Pick a starting point. You can edit everything before sending.</p><div className="panel"><div className="quick simpleQuick">{items.map(([title,heading,body])=><button key={title} onClick={()=>send({title:heading,message:body})}><MessageSquare/><div><b>{title}</b><small>{heading} — {body}</small></div><ChevronRight/></button>)}</div></div></section>}
function Notifications({data,send,refresh}){return <section className="page"><div className="eyebrow"><Bell size={13}/>MESSAGES</div><div className="pageTitle"><div><h1>Notifications</h1><p>Messages you've sent and their delivery results.</p></div><button className="primary" onClick={send}><Send size={16}/>Send notification</button></div>{data.scheduled?.length>0&&<div className="panel"><Head title="Scheduled"/>{data.scheduled.map(x=><Row key={x.id} t={x.title} a={x.audience} s="scheduled" c={`Sends ${new Date(x.sendAt).toLocaleString()}`}/>)}</div>}<div className="panel">{data.recent.length?data.recent.map(x=><Row key={x.id} t={x.title} a={x.audience} s={x.scheduled?"sent":x.status} c={`${x.successCount||0} sent · ${x.failureCount||0} failed · ${x.clicks||0} clicked`}/>):<Empty text="No messages yet." action="Send notification" onClick={send}/>}<button className="secondary" onClick={refresh}><RefreshCw size={15}/>Refresh</button></div></section>}
function Subscribers({data,notify}){const [copied,setCopied]=useState("");const key=data.registration?.workspaceKey||"";const endpoint=data.registration?.endpoint||"/api?action=registerDevice";const apiBase=`${location.origin}/api`;const scriptTag=`<script src="${location.origin}/wynotify-register.js" data-workspace-key="${key}" data-api="${apiBase}" data-label="Get updates" async></script>`;const metaTag=`<meta name="wynotify-workspace-key" content="${key}">
<meta name="wynotify-api" content="${apiBase}">`;const sdkSnippet=`<!-- 1. Copy wynotify-push-sw.js to your website root -->
<!-- 2. Add this one-line subscriber button -->
${scriptTag}`;const apiSnippet=`fetch("${endpoint}", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    workspaceKey: "${key}",
    token: pushRegistrationToken,
    subscriberType: "customers",
    tags: ["new-users"]
  })
})`;const copy=async(v,label)=>{try{await navigator.clipboard.writeText(v);setCopied(label);notify(`${label} copied`)}catch{notify("Clipboard access is unavailable")}};const downloadFile=(name,contents,type)=>{const blob=new Blob([contents],{type});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);notify(`${name} downloaded`)};const downloadWorker=async()=>{try{const r=await fetch("/wynotify-push-sw.js");if(!r.ok)throw Error("Could not load the push worker.");downloadFile("wynotify-push-sw.js",await r.text(),"text/javascript")}catch(e){notify(e.message||"Could not download the push worker.")}};return <section className="page"><div className="eyebrow"><Users size={13}/>AUDIENCE</div><h1>Subscribers</h1><p className="lead">Grow your audience from your website or app. Visitors can subscribe with one click and you can message them from WyNotify.</p><div className="subscriberHero panel"><div><span className="bigNumber">{data.subscribers}</span><span>active subscribers</span></div><div><small>{data.subscription?.plan||"Free"} plan capacity</small><div className="capacityBar"><span style={{width:`${Math.min(100,(data.subscribers/({free:1000,starter:5000,pro:25000,business:100000}[data.subscription?.plan]||1000))*100)}%`}}/></div></div></div><div className="panel developerBox"><div className="eyebrow"><Globe size={13}/>EASIEST WEBSITE SETUP</div><h2>Add a “Get updates” button to your website</h2><p>Your developer only needs to copy the push worker file to the website root and paste the script below. Visitors will see a button, allow notifications, and become subscribers automatically.</p><div className="notice"><ShieldCheck size={17}/><div><b>Works for normal websites</b><small>WordPress, custom HTML, blogs, stores and most HTTPS sites.</small></div></div><h3>Step 1 — Copy the push worker</h3><p>Download this file and place it at the root of the website as <code>/wynotify-push-sw.js</code>.</p><button className="secondary" onClick={downloadWorker}><Download size={15}/>Download push worker</button><h3>Step 2 — Add this script</h3><pre className="codeBlock">{scriptTag}</pre><div className="buttonRow"><button className="primary" onClick={()=>copy(scriptTag,"Website script")}><Copy size={15}/>Copy website script</button><button className="secondary" onClick={()=>copy(metaTag,"Meta tags")}><Copy size={15}/>Copy meta tags</button></div><h3>Optional — use meta tags</h3><p>If your site builder makes script attributes difficult, add these inside <code>&lt;head&gt;</code>. The WyNotify script can read them.</p><pre className="codeBlock">{metaTag}</pre><h3>For your developer — SDK/API</h3><p>For Android, iOS, custom apps or advanced integrations, keep using the developer API/SDK below.</p><pre className="codeBlock">{apiSnippet}</pre><div className="buttonRow"><button className="secondary" onClick={()=>copy(apiSnippet,"API example")}><Copy size={15}/>Copy API example</button><button className="secondary" onClick={()=>copy(sdkSnippet,"Website integration")}><Copy size={15}/>Copy integration</button></div>{copied&&<small>✓ {copied}</small>}</div></section>}
function Billing({notify,data,refresh}){const [email,setEmail]=useState(""),[phone,setPhone]=useState(""),[loading,setLoading]=useState(""),[status,setStatus]=useState("");useEffect(()=>{const ref=new URLSearchParams(location.search).get("reference");if(!ref)return;(async()=>{setStatus("Verifying payment…");try{const r=await apiFetch(`verifyPayment&reference=${encodeURIComponent(ref)}`);const d=await r.json();if(!r.ok)throw Error(d.error);setStatus(d.status==="succeeded"?`Payment confirmed — ${d.plan} is active.`:`Payment status: ${d.status}`);await refresh();history.replaceState({},"",location.pathname+"#Billing")}catch(e){setStatus(e.message)}})()},[]);const pay=async p=>{if(p.id==="free")return;if(!email.trim())return notify("Enter your billing email first.");setLoading(p.id);try{const r=await apiFetch("opay",{method:"POST",body:JSON.stringify({email,phone,name:email.split("@")[0],amount:Number(p.price.replace(/[₦,]/g,"")),currency:"NGN",plan:p.id})});const d=await r.json();if(!r.ok)throw Error(d.error);const link=d.data?.data?.next_action?.redirect_url?.url||d.data?.next_action?.redirect_url||d.data?.redirect_url;if(!link)throw Error("Flutterwave did not return a payment redirect.");location.href=link}catch(e){notify(e.message.replace(/Firebase/gi,"notification service"))}finally{setLoading("")}};return <section className="page"><div className="eyebrow"><CreditCard size={13}/>PLAN</div><h1>Choose the plan that fits your business</h1><p className="lead">Start free. Upgrade when you need more subscribers. Billed in naira, no card required for Free — everything Firebase gives your developer, plus click tracking and scheduled sends your business team can use directly.</p>{status&&<div className="notice"><ShieldCheck size={17}/>{status}</div>}<div className="billingTop panel"><div><Lock size={17}/><div><b>Secure checkout</b><small>Payments are handled by Flutterwave.</small></div></div><div className="billingForm"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Billing email" type="email"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone (optional)"/></div></div><div className="plans">{plans.map(p=><div className={`plan ${p.id==="pro"?"featured":""}`} key={p.id}>{p.id==="pro"&&<span className="popular">Popular</span>}<h2>{p.name}</h2><strong>{p.price}<small>/month</small></strong><em>{p.usd}/month reference</em><div>{p.features.map(f=><span key={f}><Check size={14}/>{f}</span>)}</div><button className={p.id==="free"?"secondary":"primary"} disabled={!!loading} onClick={()=>pay(p)}>{loading===p.id?"Starting…":p.id==="free"?(data.subscription?.plan||"free")==="free"?"Current plan":"Free":"Continue to payment"}</button></div>)}</div></section>}
function Composer({close,sent,initial={},capabilities={}}){const [t,setT]=useState(initial.title||""),[m,setM]=useState(initial.message||""),[audience,setAudience]=useState(initial.audience||"all"),[schedule,setSchedule]=useState(false),[scheduleAt,setScheduleAt]=useState("");const canSchedule=!!capabilities?.scheduling;const minLocal=new Date(Date.now()+5*60000).toISOString().slice(0,16);const payload=()=>{const p={title:t,message:m,audience};if(schedule&&canSchedule&&scheduleAt)p.scheduledFor=new Date(scheduleAt).toISOString();return p};return <div className="modal" onMouseDown={e=>e.target===e.currentTarget&&close()}><div className="composer"><div className="composerHead"><div><div className="eyebrow"><Send size={13}/>NEW MESSAGE</div><h2>Send a notification</h2><p>Write it like a normal message. Your customers will receive it as a push notification.</p></div><button className="icon" onClick={close}><X/></button></div><label>Title<input autoFocus value={t} onChange={e=>setT(e.target.value)} placeholder="Example: New offer available 🎉"/></label><label>Message<textarea value={m} onChange={e=>setM(e.target.value)} placeholder="Tell your customers what they need to know…" rows="5"/></label><label>Who should receive it?<select value={audience} onChange={e=>setAudience(e.target.value)}><option value="all">Everyone</option><option value="customers">Customers</option><option value="new-users">New users</option><option value="vip">VIP</option></select></label><label className="scheduleToggle"><input type="checkbox" checked={schedule} disabled={!canSchedule} onChange={e=>setSchedule(e.target.checked)}/>Schedule for later{!canSchedule&&<small> — Pro and Business plans</small>}</label>{schedule&&canSchedule&&<label>Send at<input type="datetime-local" min={minLocal} value={scheduleAt} onChange={e=>setScheduleAt(e.target.value)}/></label>}<div className="preview"><small>PREVIEW</small><div><span className="logo"><Bell size={14}/></span><article><b>{t||"Your notification title"}</b><p>{m||"Your message will appear here."}</p></article></div></div><footer><button className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={!t.trim()||!m.trim()||(schedule&&canSchedule&&!scheduleAt)} onClick={()=>sent(payload())}><Send size={16}/>{schedule&&canSchedule&&scheduleAt?"Schedule message":"Send now"}</button></footer></div></div>}
function Backup({data,notify}){const downloadBackup=()=>{const blob=new Blob([JSON.stringify({version:4,exportedAt:new Date().toISOString(),workspace:{subscription:data.subscription||{},recent:data.recent||[]}},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`wynotify-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href);notify("Backup downloaded")};return <section className="page panel"><div className="eyebrow"><Globe size={13}/>ADVANCED / DATA</div><h1>Backup & Restore</h1><p>Export a safe workspace snapshot. Secrets and device push tokens are never placed in the backup.</p><div className="backupGrid"><div><b>Backup now</b><p>Download a portable JSON snapshot.</p><button className="primary" onClick={downloadBackup}><Download size={16}/>Download backup</button></div><div><b>Validate backup</b><p>Check a WyNotify JSON backup before using it.</p><input type="file" accept="application/json,.json" onChange={async e=>{const f=e.target.files?.[0];if(!f)return;try{const x=JSON.parse(await f.text());if(x?.version!==4||!x?.workspace)throw new Error("This is not a compatible WyNotify backup.");notify("Backup is valid.")}catch(err){notify(err.message||"Invalid backup file.")}}}/></div></div></section>}
function ExportData({data,notify}){const exp=()=>{const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="wynotify-data.json";a.click();URL.revokeObjectURL(a.href);notify("Data exported")};return <section className="page panel"><div className="eyebrow"><Download size={13}/>ADVANCED / DATA</div><h1>Export data</h1><p>Download the workspace data currently loaded in this dashboard.</p><button className="primary" onClick={exp}><Download size={16}/>Export JSON</button></section>}
function SettingsPage({dark,setDark}){return <section className="page panel"><div className="eyebrow"><Settings size={13}/>ACCOUNT</div><h1>Settings</h1><div className="setting"><div><b>Dark mode</b><small>Use the low-glare interface.</small></div><button className="secondary" onClick={()=>setDark(!dark)}>{dark?"Enabled":"Disabled"}</button></div></section>}
function InfoPage({page,nav}){const map={Segments:"Group subscribers by type or tags so you can send to the right people.",Tags:"Labels such as vip, new-user or customers can be attached during registration.",Analytics:"See how many devices your messages reached. More advanced reporting can be added later.","Notification Performance":"Delivery counts show how many devices successfully received each notification.",API:"Developer endpoints for registration, sending, dashboard data and billing. Normal business owners do not need to use these.",SDK:"The SDK/integration tools let your developer connect your website or app while you keep using the simple WyNotify dashboard.",Webhooks:"Payment events are handled automatically. This page is kept under Advanced tools for developer reference.",Integrations:"Connect your website or app once, then manage customer notifications from WyNotify. Billing is handled securely through our payment partner.","Help Center":"Start with Connect app, register a test device, then send yourself a test notification.","Contact Support":"Need help with your workspace, billing or integration? Contact support."};return <section className="page panel"><div className="eyebrow"><BookOpen size={13}/>ADVANCED</div><h1>{page}</h1><p className="lead">{map[page]||"Advanced workspace tools."}</p>{page==="Contact Support"&&<a className="primary" href={`mailto:${import.meta.env.VITE_SUPPORT_EMAIL||"support@example.com"}`}>Email support</a>}{page==="API"&&<div className="featureList"><div><Check size={15}/>POST /api?action=registerDevice — register a subscriber</div><div><Check size={15}/>POST /api?action=send — send a notification (add scheduledFor on Pro/Business to send later)</div><div><Check size={15}/>GET /api?action=dashboard — load workspace stats</div><div><Check size={15}/>POST /api?action=opay — start a plan payment</div></div>}{page==="SDK"&&<button className="primary" onClick={()=>nav("Subscribers")}><Code2 size={16}/>Open SDK instructions</button>}</section>}
function Bottom({page,nav,menu}){return <nav className="bottom mobile">{[["Dashboard",Home],["Notifications",Bell],["Subscribers",Users],["Billing",CreditCard]].map(([n,I])=><button className={page===n?"active":""} key={n} onClick={()=>nav(n)}><I/><small>{n==="Dashboard"?"Home":n}</small></button>)}<button onClick={menu}><Menu/><small>More</small></button></nav>}
createRoot(document.getElementById("root")).render(<App/>);
